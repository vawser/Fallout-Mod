namespace = economy_selection
namespace = economy_flavour

country_event = {
	id = economy_selection.1
	title = economy_selection.1.title
	desc = economy_selection.1.desc
	picture = GOV_RUINS_CLEAR_eventPicture
	
	is_triggered_only = yes

	option = {
		name = economy_selection.1.option.a
        
        if = {
            limit = {
                has_traditional_economy = yes
            }
            custom_tooltip = ALREADY_TRADITIONAL_ECONOMY
        }
        else = {
            set_traditional_economy = yes
			add_adm_power = -100
            custom_tooltip = TRADITIONAL_ECONOMY_EXPLANATION
            add_civil_unrest_5 = yes
        }
	}
	option = {
		name = economy_selection.1.option.b
        
        if = {
            limit = {
                has_command_economy = yes
            }
            custom_tooltip = ALREADY_COMMAND_ECONOMY
        }
        else = {
            set_command_economy = yes
			add_adm_power = -100
            custom_tooltip = COMMAND_ECONOMY_EXPLANATION
            add_civil_unrest_5 = yes
        }
	}
	option = {
		name = economy_selection.1.option.c
		
        if = {
            limit = {
                has_market_economy = yes
            }
            custom_tooltip = ALREADY_MARKET_ECONOMY
        }
        else = {
            set_market_economy = yes
			add_adm_power = -100
            custom_tooltip = MARKET_ECONOMY_EXPLANATION
            add_civil_unrest_5 = yes
        }
	}
	option = {
		name = economy_selection.1.option.d
        
        if = {
            limit = {
                has_mixed_economy = yes
            }
            custom_tooltip = ALREADY_MIXED_ECONOMY
        }
        else = {
            set_mixed_economy = yes
			add_adm_power = -100
            custom_tooltip = MIXED_ECONOMY_EXPLANATION
            add_civil_unrest_5 = yes
        }
	}
	option = {
		name = event_exit
	}
}

