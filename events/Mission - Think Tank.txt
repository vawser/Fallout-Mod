namespace = mission_think_tank

country_event = {
	id = mission_think_tank.1
	title = "mission_think_tank.1.title"
	desc = "mission_think_tank.1.desc"
    picture = USA_FLAG_eventPicture

    is_triggered_only = yes
    
	trigger = {
        
	}

    immediate = {
        hidden_effect = {
            clr_country_flag = mission_active
        }
    }
    
	option = {
		name = mission_think_tank.1.option.a
        
        add_adm_power = 100
	}	
}
