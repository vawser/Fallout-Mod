namespace = firearms_selection
namespace = firearms_flavour

country_event = {
	id = firearms_selection.1
	title = firearms_selection.1.title
	desc = firearms_selection.1.desc
	picture = GOV_RUINS_CLEAR_eventPicture
	
	is_triggered_only = yes

	option = {
		name = firearms_selection.1.option.a
        
        if = {
            limit = {
                has_firearms_allowed = yes
            }
            custom_tooltip = ALREADY_ALLOW_FIREARMS
        }
        else = {
            set_firearms_allowed = yes
			add_dip_power = -100
            custom_tooltip = FIREARMS_ALLOWED_EXPLANATION
            add_civil_unrest_3 = yes
        }
	}
	option = {
		name = firearms_selection.1.option.b
        
        if = {
            limit = {
                has_firearms_licensed = yes
            }
            custom_tooltip = ALREADY_LICENSE_FIREARMS
        }
        else = {
            set_firearms_licensed = yes
			add_dip_power = -100
            custom_tooltip = FIREARMS_LICENSED_EXPLANATION
            add_civil_unrest_3 = yes
        }
	}
	option = {
		name = firearms_selection.1.option.c
		
        if = {
            limit = {
                has_firearms_forbidden = yes
            }
            custom_tooltip = ALREADY_FORBID_FIREARMS
        }
        else = {
            set_firearms_forbidden = yes
			add_dip_power = -100
            custom_tooltip = FIREARMS_FORBIDDEN_EXPLANATION
            add_civil_unrest_3 = yes
        }
	}
	option = {
		name = event_exit
	}
}

