namespace = law_selection
namespace = law_flavour

country_event = {
	id = law_selection.1
	title = law_selection.1.title
	desc = law_selection.1.desc
	picture = GOV_RUINS_CLEAR_eventPicture
	
	is_triggered_only = yes

	option = {
		name = law_selection.1.option.a
        
        if = {
            limit = {
                has_democratic_law = yes
            }
            custom_tooltip = ALREADY_DEMOCRATIC_LAW
        }
        else = {
            set_democratic_law = yes
			add_adm_power = -100
            custom_tooltip = DEMOCRATIC_LAW_EXPLANATION
            add_civil_unrest_5 = yes
        }
	}
	option = {
		name = law_selection.1.option.b
        
        if = {
            limit = {
                has_oligarchic_law = yes
            }
            custom_tooltip = ALREADY_OLIGARCHIC_LAW
        }
        else = {
            set_oligarchic_law = yes
			add_adm_power = -100
            custom_tooltip = OLIGARCHIC_LAW_EXPLANATION
            add_civil_unrest_5 = yes
        }
	}
	option = {
		name = law_selection.1.option.c
		
        if = {
            limit = {
                has_autocratic_law = yes
            }
            custom_tooltip = ALREADY_AUTOCRATIC_LAW
        }
        else = {
            set_autocratic_law = yes
			add_adm_power = -100
            custom_tooltip = AUTOCRATIC_LAW_EXPLANATION
            add_civil_unrest_5 = yes
        }
	}
	option = {
		name = event_exit
	}
}

