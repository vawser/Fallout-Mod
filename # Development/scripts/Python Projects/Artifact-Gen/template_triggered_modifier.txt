# {artifact_name}
artifact_{artifact} = [
	potential = [
        has_country_flag = artifact_{artifact}
    ]
    trigger = [
        custom_trigger_tooltip = [
            tooltip = IS_PRIMARY_ARTIFACT_{artifact}
            has_country_flag = primary_artifact_{artifact}
        ]
    ]

    # ADD MODIFIER
]

